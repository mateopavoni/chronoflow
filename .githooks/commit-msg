#!/bin/sh
# hard block on AI co-author trailers surviving into a commit.
# Regla del repo: commits a nombre de Mateo, nunca "Co-Authored-By: Claude/Anthropic".
msg_file="$1"
tmp="$(mktemp)"
grep -viE '^Co-Authored-By:.*(claude|anthropic)' "$msg_file" > "$tmp"
mv "$tmp" "$msg_file"
